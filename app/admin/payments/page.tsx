import { requireAdmin } from '@/lib/security'
export default function AdminPayments(){return <main className="wrap section"><h1>Payment Operations</h1><p className="muted">Verified transaction references are stored once. qBirr verification remains server-side.</p></main>}
