'use client'
import {useState} from 'react'
export function AdminStoreForm(){const [msg,setMsg]=useState('');const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const f=new FormData(e.currentTarget);const body={title:String(f.get('title')||''),description:String(f.get('description')||''),category:String(f.get('category')||''),price:Number(f.get('price')||0),imageUrl:String(f.get('imageUrl')||'')||undefined};const r=await fetch('/api/admin/store/products',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();setBusy(false);setMsg(r.ok?'Product created.':(d.error||'Unable to create product'));if(r.ok)e.currentTarget.reset()}return <form className="form card" onSubmit={submit} style={{marginTop:18}}><input name="title" placeholder="Product title" required/><textarea name="description" placeholder="Description" required/><input name="category" placeholder="Category" required/><input name="price" type="number" min="0" placeholder="Price ETB" required/><input name="imageUrl" placeholder="Public image URL (optional)"/><button className="btn primary" disabled={busy}>{busy?'Saving…':'Create product'}</button>{msg&&<span className="small muted">{msg}</span>}</form>}
